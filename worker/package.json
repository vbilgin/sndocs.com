{
  "name": "sndocs-cloudflare-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "wrangler deploy --dry-run --env preview && wrangler deploy --dry-run --env production"
  },
  "devDependencies": {
    "wrangler": "4.114.0"
  }
}
